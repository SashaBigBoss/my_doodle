import React from 'react';
import { HeaderContainer, SiteTitle } from '../styles/HeaderStyles';

const Header = () => {
  return (
    <HeaderContainer>
      <SiteTitle>My Doodle</SiteTitle>
    </HeaderContainer>
  );
};

export default Header;
